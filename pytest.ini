[pytest]
# Disable Gaudi's CTest pytest plugins — they conflict with standalone pytest runs
# (collect_for_ctest crashes with AttributeError: 'Session' has no 'ctest_args').
addopts = -p no:collect_for_ctest -p no:ctest_measurements_reporter -p no:gaudi_fixtures
