I want to modify the SiW-ECAL geometry to model realistic silicon tiles separated by dead regions.

Current implementation:
- The detector uses CartesianGridXY segmentation.
- Cell size is 5.625 × 5.625 mm².
- The sensitive surface is continuous (no dead area between cells).

Target implementation:
- Replace the current CartesianGridXY segmentation with TiledLayerGridXY.
- The active silicon cell size must become 5.5 × 5.5 mm².
- There must be a 1.0 mm inactive gap between neighbouring cells in both X and Y directions.

This means that:
- The active silicon area is 5.5 mm × 5.5 mm.
- Adjacent cells are separated by a physical dead region of 1.0 mm.
- Particles crossing the gap should not produce hits.

Please do not implement this by only changing the segmentation parameters. The geometry itself must contain the dead regions. The segmentation should simply reflect the geometry.

Tasks:
1. Replace the current CartesianGridXY readout with TiledLayerGridXY.
2. Modify the detector geometry so that each sensitive silicon tile has dimensions 5.5 × 5.5 mm².
3. Introduce a 1.0 mm physical gap (air or non-sensitive material) between neighbouring tiles.
4. Ensure the cell IDs remain consistent and uniquely identify every sensitive tile.
5. Update all XML parameters and C++ geometry construction as required.
6. Verify that there are no overlaps or gaps other than the intended dead regions.
7. Keep all other detector dimensions and materials unchanged.

After implementing the changes, explain:
- Which XML parameters were modified.
- Which C++ files were changed.
- How the dead regions are created.
- Why TiledLayerGridXY is now the appropriate segmentation.